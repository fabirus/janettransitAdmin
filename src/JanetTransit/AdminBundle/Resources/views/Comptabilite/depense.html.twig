{% extends '::base.html.twig' %}

{% block title %} {{ parent() }} | Liste des Dépenses mensuels {% endblock %}

{% block body -%}
    <div class="alert alert-info" role="alert" style="font-size: 18px">Comptabilité en  Dépense de la période du <b>{{ entity.periode }}</b> </div>
    <div class="row">
        <div class="col-md-6">
            <a href="{{ path('comptabilite_show', {'id' : entity.id}) }}">
                <i class="fa fa-backward fa-3x" title="Revenir à la liste" style="color:
        #0FAD8C;cursor: pointer;"></i>
            </a>
        </div>
        <div class="col-md-6">
            <i title="Imprimer" class="fa fa-print fa-3x" onclick="common().print('noprint', '')" style="float: right; cursor: pointer; margin-right: 2%"></i>
        </div>
    </div><br />

{% if entities is not empty %}
    <div class="print">
        <div class="panel panel-success">
            <div class="panel-heading"><h4><b>Bilan Comptable En Dépense de la période du {{ entity.periode }}</b></h4></div>
            <table class="table table-hover table-bordered">
                <tbody>
                    <tr>
                        <th>Montant Total des Dépenses</th>
                        <td>{{ total}} <b>FCFA</b></td>
                    </tr>
                    {% for key, data in resultat %}
                        <tr>
                            <th>Montant Total des Dépenses de Type <b>{{ key|upper }}</b></th>
                            <td>{{ data }} <b>FCFA</b></td>
                        </tr>
                    {% endfor %}
                    <tr>
                        <th>Montant des Dépenses Payées</th>
                        <td>{{ paye}} <b>FCFA</b></td>
                    </tr>
                    <tr>
                        <th>Montant des Dépenses Non Payées</th>
                        <td>{{ Nonpaye }} <b>FCFA</b></td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
{% else %}
    <div class="row" style="text-align: center; font-size: 30px">
        <i class="fa fa-dashboard fa-5x"></i><br>
        Aucune Carburation
    </div>
{% endif %}

{% endblock %}


{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript" src="{{ asset('js/lib/common.js') }}"></script>
{% endblock %}
