{% extends '::base.html.twig' %}

{% block body -%}
    <div class="alert alert-info" role="alert" style="font-size: 18px">Modification du Poste {{ entity.nom }}</div>
    <div>
        <a href="{{ path('poste_informations') }}">
            <i class="fa fa-backward fa-3x" title="Revenir à la liste" style="color:
        #0FAD8C;cursor: pointer;"></i>
        </a>
    </div><br>

{{ form_start(edit_form) }}
    {{ form_errors(edit_form) }}

    {{ form_row(edit_form.nom) }}
    {{ form_row(edit_form.service) }}
    {{ form_row(edit_form.description) }}

    {{ form_widget(edit_form.submit, { 'label': 'Enregister', 'attr' :{'type' : 'button', 'class' :'btn btn-primary register registerPoste'}})}}

    {{ form_end(edit_form) }}

{% endblock %}


{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript"  src="{{ asset('js/lib/common.js')}}"></script>
    <script>
        common().initPopover();
        $("form").validate();
        common().validation();
    </script>

{% endblock %}
