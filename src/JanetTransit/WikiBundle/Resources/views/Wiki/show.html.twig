{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Article {% endblock%}

{% block body %}
    <div class="alert alert-info" role="alert" style="font-size: 18px"><b><i>{{ article.title }}</i></b></div>
    <a href="{{ path('janet_transit_wiki_articles', {'id' : idCat}) }}">
        <i class="fa fa-backward fa-3x" title="Revenir à la liste" style="color:
        #0FAD8C;cursor: pointer;"></i>
    </a><br><br><br>
    <div class="row">
        <div class="col-md-12">
            <div style="min-height: 300px" class="panel panel-info">
                <div class="panel-body">
                    <div class="media" style="">
                        <div class="media-left" style="width: 20pc;padding-left: 10px;">
                            <img height="140px" width="140px" id="image"
                                 class="media-object" src="{{ app.request.basePath }}{{ vich_uploader_asset(article, 'articleFile') }}"
                                 title="{{ article.title }}">
                        </div>
                        <div class="media-body">
                            {{ article.content|raw }}
                        </div>
                    </div>
                    <img width="500px" height="500px" id="imageSize" style="display: none"
                         src="{{ app.request.basePath }}{{ vich_uploader_asset(article, 'articleFile') }}}"
                         title="{{ article.title }}">
                </div>
            </div>
        </div>
    </div>

    {#<div class="media" style="background-color: #ECEDEE;margin-left: auto;margin-right: auto;width: 80%;">#}
        {#<div class="media-left" style="width: 20pc;padding-left: 10px;">#}
            {#<img id="image" class="media-object" src="{% path article.image, 'small' %}" title="{{ article.title }}" data-zoom-image="{% path article.image, 'wiki' %}">#}
        {#</div>#}
        {#<div class="media-body">#}
            {#<h4 class="media-heading"><b>{{ article.title }}</b></h4>#}
            {#{{ article.content|raw }}#}
        {#</div>#}
    {#</div>#}
    {#<img id="imageSize" style="display: none" src="{% path article.image, 'wiki' %}" title="{{ article.title }}">#}


{% endblock %}

{% block javascripts %}
    <script src="{{ asset('assets/vendor/jquery/dist/jquery.js')}}"></script>
    <script src="{{ asset('assets/vendor/bootstrap/dist/js/bootstrap.min.js')}}"></script>
    <script src="{{ asset('js/jquery.metisMenu.js') }}"></script>
    <script src="{{ asset('js/custom.js') }}"></script>
    <script src="{{ asset('js/lib/external/zoom-elevator/jquery.elevateZoom-3.0.8.min.js') }}"></script>
    <script>
        $(window).load(function() {
            $(".loader").fadeOut("100000");
        });
        var image = $('#imageSize');
        $("#image").elevateZoom({
            tint:true,
            tintColour:'#F90',
            tintOpacity:0.5,
            scrollZoom : true,
//            zoomType: "inner",
            cursor: "crosshair",
            zoomWindowFadeIn: 10,
            zoomWindowFadeOut: 10,
            zoomWindowWidth: image.width(),
            zoomWindowHeight: image.height()
        });
    </script>
{% endblock %}